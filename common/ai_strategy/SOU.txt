SOU_focus_on_your_enemies = {

    reversed = yes

	enable = {
		has_global_flag = second_russian_civil_war_in_progress
        NOT = {
            has_war_with = SOU
        }
	}

	enable_reverse = {
        has_global_flag = second_russian_civil_war_in_progress
        is_in_array = { global.russian_scw_participants = SOU }
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = conquer
		id = SOU
		value = -9999
	}

	ai_strategy = {
		type = antagonize
		id = SOU
		value = -9999
	}

	ai_strategy = {
		type = declare_war
		id = SOU
		value = -9999
	}

    ai_strategy = {
		type = ignore
		id = SOU
		value = 9999
	}
}

SOU_focus_only_scw  = {

	allowed = {
		original_tag = SOU
	}

	reversed = yes

	enable = {
		has_war_with = SOU
	}

	enable_reverse = {
		tag = SOU
		has_global_flag = second_russian_civil_war_in_progress
		is_in_array = { global.russian_scw_participants = SOU }
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control

		priority = 1000

		tag = SOU

		ordertype = front
		execute_order = yes
		manual_attack = yes
	}

	ai_strategy = {
		type = front_unit_request

		tag = SOU

		value = 1000
	}
}